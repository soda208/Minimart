namespace MinimartMIS
{
    internal static class Program
    {
        /// <summary>
        ///  The main entry point for the application.
        /// </summary>
        [STAThread]
        static void Main()
        {
            // To customize application configuration such as set high DPI settings or default font,
            // see https://aka.ms/applicationconfiguration.


            //ApplicationConfiguration.Initialize();
            //Application.Run(new frmProducts());

            //ApplicationConfiguration.Initialize();
            //Application.Run(new frmCategory());


            //ApplicationConfiguration.Initialize();
            //Application.Run(new frmEmployees());


            //ApplicationConfiguration.Initialize();
            //Application.Run(new frmPOS());


            //Application.Run(new frmProduct_by_Category());
            
            //Application.Run(new frmEmployees_Sales());

            Application.Run(new frmMinimart());
        }
    }
}